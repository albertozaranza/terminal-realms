/** Estatísticas gerais acumuladas durante a partida. */
export interface Statistics {
  enemiesDefeated: number;
  bossesDefeated: number;
  totalGoldEarned: number;
  totalExperienceEarned: number;
  deaths: number;
}
